<!--
*   FILE          : StatSliderControl.xaml
*   PROJECT       : D2ArmorCalc
*   PROGRAMMER    : ExoVizzy
*   FIRST VERSION : May 17, 2026
*   DESCRIPTION   : Reusable UserControl for single stat row, containing
*                   stat label, min textbox, range slider,
*                   & max textbox with two-way live binding.
-->
<UserControl x:Class="D2ArmorCalc.StatSliderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:D2ArmorCalc">

    <Grid Margin="0,4,0,4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"  /> <!-- Stat label -->
            <ColumnDefinition Width="80"  /> <!-- Min label + textbox -->
            <ColumnDefinition Width="*"   /> <!-- Slider -->
            <ColumnDefinition Width="80"  /> <!-- Max label + textbox -->
        </Grid.ColumnDefinitions>

        <!-- Stat Label -->
        <TextBlock Grid.Column="0"
                   Style="{StaticResource BodyText}"
                   Text="{Binding Label}"
                   VerticalAlignment="Center"
                   Margin="6,0,0,0" />

        <!-- Min Input -->
        <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0">
            <TextBlock Text="Min:"
                       Style="{StaticResource SecondaryText}"
                       VerticalAlignment="Center"
                       Margin="0,0,4,0" />
            <TextBox Width="46"
                     Style="{StaticResource InputTextBox}"
                     FontSize="11"
                     Text="{Binding MinValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center" />
        </StackPanel>

        <!-- Range Slider -->
        <local:RangeSlider Grid.Column="2"
                           MinValue="{Binding MinValue, Mode=TwoWay}"
                           MaxValue="{Binding MaxValue, Mode=TwoWay}"
                           Stat="{Binding Stat}"
                           VerticalAlignment="Center"
                           Margin="4,0,4,0" />

        <!-- Max Input -->
        <StackPanel Grid.Column="3"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="8,0,0,0">
            <TextBlock Text="Max:"
                       Style="{StaticResource SecondaryText}"
                       VerticalAlignment="Center"
                       Margin="0,0,4,0" />
            <TextBox Width="46"
                     Style="{StaticResource InputTextBox}"
                     FontSize="11"
                     Text="{Binding MaxValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>